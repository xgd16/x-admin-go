
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  gen:
    dao:
      - link: "mysql:root:486213@tcp(127.0.0.1:3306)/yi?charset=utf8mb4&parseTime=true&loc=Local"
        tables: "sys_admin"
        descriptionTag: true

  docker:
    build: "-a amd64 -s linux -p temp -ew"
    tagPrefixes:
      - my.image.pub/my-app